package Lab1Demo;

import java.io.File; // this is for dealing I/O file.
import java.io.IOException;
import java.util.Scanner; // this lib is for read the data from file.
import java.util.StringTokenizer; // this is to help with splitting data line.

// 1. open and read the input.txt file line by line
// 2. Create a that Food object for each data line
// 3. Push the object into your own stack
// 4. Test the push, pop, peek and display methods.


public class Main {
    
    public static void main(String args[]) throws IOException {        
        // File f = new File("input.txt");
        // StringTokenizer st;
        // Stack myStack = new Stack(10);

    
        // Scanner sc = new Scanner(f);

        // while(sc.hasNextLine()) {
        //     String dataLine = sc.nextLine();
        //     st = new StringTokenizer(dataLine, ",");
        //     Food myFood = new Food(Integer.parseInt(st.nextToken()), st.nextToken(), Double.parseDouble(st.nextToken()));
        //     myStack.push(myFood);
            
        // }// end while loop
        // sc.close();

        // //myStack.peek();
        // myStack.pop();
        // myStack.peek();
        // myStack.display();

    
    // 1. open and read the iris.csv file line by line
    // 2. Create a that Flower object for each data line
    // 3. Push the object into your own Queue
    // 4. Test the enqueue, dequeue and display methods.

    }// end main
}// end class Main
